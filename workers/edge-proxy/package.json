{
  "name": "moodtrip-edge-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edge proxy for MoodTrip (Phase 0a ship-stop security)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "~5.7.2",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
